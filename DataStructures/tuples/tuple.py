# read only List

point = (1, 2)
print(type(point))

point_another = 1,
print(type(point))

add = point + point_another


mul = (1, 2) * 3

list = tuple([1, 2, 3])  # We can pass any iterable


# tuples are immutable
