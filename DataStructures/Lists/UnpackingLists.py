numbers = [1, 2, 3, 4, 4, 4, 4]
# Unpacking only first and secind element of that list
first, second, *other = numbers


first, second, third = numbers  # Unpacking the List

# first = numbers[0]
# second = numbers[1]

print(first)
